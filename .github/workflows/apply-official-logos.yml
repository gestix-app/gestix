name: Apply official integration logos

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  logos:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply logos
        run: python scripts/add-official-logos.py
      - name: Commit updated website
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Add official integration logos to Gestix web'
          git push
